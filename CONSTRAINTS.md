# قواعد تقنية ملزمة

قرارات صادرة عن المالك. **أي كود يخالفها يُعتبر خطأً**، حتى لو اشتغل.

> **مصدر الحقيقة الوحيد لقواعد العمل هو [SPEC.md](SPEC.md).**
>
> كل إشارة في هذا الملف إلى «القسم 3» أو «القسم 5.3» أو «القسم 5.6» أو «القسم 6.1» أو «القسم 6.3» أو «القسم 6.4» تعني ذلك القسم **في SPEC.md**.
>
> هذا الملف لا يعرّف أي قاعدة عمل ولا يعدّلها — هو يضيف قيوداً تقنية على *طريقة تنفيذ* ما ورد في SPEC.md فقط.
>
> **قاعدة دائمة:** أي تعديل مستقبلي على قواعد العمل يُكتب في SPEC.md أولاً، ثم يُنفَّذ في الكود. إذا اختلف الكود عن SPEC.md فالكود هو الخطأ.

---

## ١. الوقت والمنطقة الزمنية

سيرفر Render بيشتغل بتوقيت **UTC**. بدون معالجة صريحة، حجز الساعة ٨ مساءً بيصير ١١ مساءً بالنظام.

**القاعدة:**

- **التخزين:** كل الطوابع الزمنية بقاعدة البيانات بـ **UTC** (`timestamptz`).
- **العرض:** كل تحويل للعرض بيمر بـ `Asia/Amman` — من متغيّر `TIMEZONE`، مش مكتوب بالكود.
- **حساب القواعد بالتوقيت المحلي حصراً:**
  - تحديد **الخميس/الجمعة/السبت** (قاعدة العائلات، القسم 5.3) — `weekday()` على تاريخ محلي، لا UTC. حجز الساعة ١١ مساءً يوم أربعاء بتوقيت عمّان هو `weekday()==2`، بس بـ UTC هو الساعة ٨ مساءً أربعاء — وبحالات أخرى ممكن ينزلق ليوم ثاني كلياً.
  - تحديد وقت **الهابي أور** (١:٠٠–٦:٠٠ مساءً، السبت–الخميس، القسم 3) — بالتوقيت المحلي.
  - فلترة **حجوزات اليوم** (`/today`، لوحة الأدمن، قاعدة اليوم الكامل بالقسم 5.6) — "اليوم" يعني اليوم بعمّان.

**التنفيذ:** دالة وحدة `now_local()` ودالة `to_local(ts)` بـ `config.py`، والباقي بيستدعيهن. ممنوع `datetime.now()` عارية بأي مكان.

---

## ٢. كل المدد الزمنية تمر عبر `TEST_TIME_SCALE`

**ولا رقم مدة مكتوب ثابت خارج هذا المتغيّر.**

| المدة | القيمة الحقيقية | المرجع |
|---|---|---|
| تذكير الزبون قبل الموعد | 30 دقيقة | 6.4 |
| سؤال الأدمن عن الحضور بعد الموعد | 10 دقائق | 6.4 |
| الإلغاء التلقائي بعد الموعد | 30 دقيقة | 6.4 |
| تنبيه الأدمن الثاني بلا رد | 15 دقيقة | 6.3 |
| صلاحية رابط الحجز | 30 دقيقة | 6.1 |

**التنفيذ:** ثابت واحد لكل مدة بـ `config.py`، وكلهن بيمروا بدالة واحدة:

```python
def minutes(n: int) -> timedelta:
    """يحوّل الدقائق لمدة، مقسومة على معامل الاختبار.

    TEST_TIME_SCALE=1  → الأوقات الحقيقية (الإنتاج).
    TEST_TIME_SCALE=60 → كل دقيقة بتصير ثانية (اختبار المرحلة 4).
    """
    return timedelta(minutes=n / TEST_TIME_SCALE)
```

أي `timedelta(minutes=...)` أو `sleep(...)` أو مقارنة زمنية بتتخطى `minutes()` = خطأ.

---

## ٣. صور الصالات

الأصول بحدود **٥٠٠٠ بكسل** — ما بتتحمّل على بيانات الموبايل.

**المطلوب بالمرحلة 3:**

- عرض أقصى **1600 بكسل** (الارتفاع بالتناسب — النسب الثلاث مختلفة: مربّعة، عرضية 1.22، طولية 0.75).
- **WebP** أساسي + **JPG** احتياطي، عبر `<picture>`:
  ```html
  <picture>
    <source srcset="images/hall_main.webp" type="image/webp">
    <img src="images/hall_main.jpg" alt="الصالة الكبيرة">
  </picture>
  ```
- **الجودة تحافظ على وضوح الطاولات.** الزبون بيضغط على نقطة فوق طاولة — لازم يميّز الطاولة تحت النقطة. جودة عدوانية بتخلّي الأرضية الحجرية عجينة والطاولات مش مميّزة. نبلّش من `quality=85` ونعاين بصرياً قبل ما ننزل.
- الأصول بتنحفظ بـ `web/images/original/` وهي **محجوبة عن گِت** (`.gitignore`).

---

## ٤. الأسرار

ولا مفتاح ينطبع بأي مكان — لا بالطرفية، لا باللوق، لا برسائل الخطأ، ولا حتى جزئياً. عند التشخيص: `SET` أو `MISSING` بس.
