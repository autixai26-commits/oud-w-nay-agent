# ملفات لا لزوم لها داخل صورة الإنتاج

__pycache__/
*.py[cod]
.venv/
venv/

# سكربتات التحقق تُشغَّل محلياً لا في الإنتاج
verify_phase*.py

# أداة ضغط الصور تُشغَّل عند تحديث الصور فقط
compress_images.py

# الأسرار لا تدخل الصورة إطلاقاً — Render يمرّرها كمتغيّرات بيئة
.env
*.env
