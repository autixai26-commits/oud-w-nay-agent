# --- الخادم ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- طلبات HTTP (تيليجرام + OpenAI + ElevenLabs، كلها بدون SDK) ---
httpx==0.28.1

# --- قاعدة البيانات ---
supabase==2.11.0

# --- الجدولة (التذكيرات والإلغاء التلقائي) ---
APScheduler==3.11.0

# --- قراءة ملف .env بالتشغيل المحلي ---
python-dotenv==1.0.1

# --- ضغط صور الصالات لأحجام مناسبة للويب ---
pillow==11.1.0
